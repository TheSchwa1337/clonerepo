# Schwabot Pipeline Configuration
# ===============================
# Unified configuration for Schwabot's mathematical pipeline
# This file consolidates all mathematical framework settings and provides
# Cursor-friendly configuration management.

# Core Pipeline Settings
pipeline:
  enabled: true
  mode: "production"  # production, development, testing
  debug: false
  log_level: "INFO"
  
  # Hardware Configuration
  hardware:
    preference: "auto"  # cpu, gpu, auto
    enable_gpu_acceleration: true
    enable_cpu_optimization: true
    memory_limit_gb: 8
    batch_size: 100
    
  # Performance Settings
  performance:
    timeout_seconds: 30.0
    max_retries: 3
    cache_enabled: true
    cache_ttl_seconds: 3600
    max_cache_size: 1000
    parallel_processing: true

# Symbolic Math Framework
symbolic_math:
  enabled: true
  
  # Symbol Aliases (Cursor-friendly)
  symbols:
    gradient: "∇"
    phase: "Ω"
    signal: "ψ"
    entropy: "λ"
    drift: "D"
    noise: "Σ"
  
  # Hardware Selection
  hardware_preference: "auto"
  enable_phantom_boost: true
  enable_context_awareness: true
  
  # Computation Limits
  max_iterations: 100
  convergence_threshold: 1e-6
  
  # Context Settings
  context:
    enable_phantom_layer: true
    enable_vault_state: true
    enable_entropy_index: true

# Two-Gram Detection
two_gram:
  enabled: true
  
  # Pattern Detection
  max_patterns: 1024
  pattern_threshold: 0.1
  burst_threshold: 0.5
  similarity_threshold: 0.8
  entropy_window: 16
  
  # Storage Settings
  pattern_history_size: 1000
  enable_similarity_cache: true
  
  # Performance
  enable_hardware_optimization: true
  batch_processing: true

# Mathematical Framework Components
mathematical_framework:
  # Recursion Configuration
  recursion:
    max_depth: 50
    convergence_threshold: 1e-6
    memoization_cache_size: 128
    enable_depth_guards: true
    enable_convergence_checking: true
    enable_memoization: true
  
  # Drift Shell Configuration
  drift_shell:
    shell_radius: 144.44
    ring_count: 12
    cycle_duration: 3.75  # minutes
    psi_infinity: 1.618033988749  # Golden ratio
    drift_coefficient: 0.1
    enable_ring_allocation: true
    enable_depth_mapping: true
  
  # Quantum Configuration
  quantum:
    energy_scale: 1.0
    planck_constant: 1.054571817e-34
    enable_phase_harmonization: true
    enable_quantum_entropy: true
    enable_wave_functions: true
  
  # Thermal Configuration
  thermal:
    thermal_conductivity: 0.024  # W/(mK) - air
    heat_capacity: 1005.0  # J/(kgK) - air
    boltzmann_constant: 1.380649e-23
    enable_thermal_pressure: true
    enable_thermal_gradients: true
    enable_entropy_mapping: true

# BTC256SHA Pipeline
btc_pipeline:
  price_history_size: 1000
  hash_history_size: 1000
  enable_price_processing: true
  enable_hash_generation: true
  enable_mathematical_analysis: true
  enable_drift_field_computation: true
  price_normalization_factor: 100000.0
  time_normalization_factor: 3600.0  # seconds to hours

# Ferris Wheel Visualization
ferris_wheel:
  time_points_count: 100
  enable_recursive_visualization: true
  enable_entropy_stabilization: true
  enable_drift_field_visualization: true
  enable_data_export: true
  export_format: "json"
  visualization_cache_size: 50

# Validation Configuration
validation:
  enable_scalar_validation: true
  enable_vector_validation: true
  enable_matrix_validation: true
  enable_tensor_validation: true
  enable_quantum_state_validation: true
  enable_operation_validation: true

# Error Handling
error_handling:
  enable_exception_logging: true
  enable_error_recovery: true
  max_retry_attempts: 3
  retry_delay: 1.0  # seconds
  enable_graceful_degradation: true
  log_error_details: true

# Logging Configuration
logging:
  log_level: "INFO"
  enable_file_logging: true
  enable_console_logging: true
  log_file_path: "logs/schwabot_pipeline.log"
  max_log_file_size: 10485760  # 10 MB
  backup_count: 5
  log_format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

# Cursor Integration Settings
cursor_integration:
  enable_symbolic_aliases: true
  enable_type_hints: true
  enable_docstrings: true
  enable_modular_calls: true
  enable_context_awareness: true
  
  # Symbol Mapping for Cursor
  symbol_mapping:
    gradient_operator: "EntropicGradient.derive"
    phase_operator: "PhaseOmega.compute"
    signal_operator: "SignalPsi.extract_field"
    entropy_operator: "SignalPsi.compute_entropy_weight"
    drift_operator: "DriftField.compute_drift"
    noise_operator: "NoiseField.sum_noise"

# System Integration
system_integration:
  enable_all_components: true
  enable_advanced_integration: true
  enable_system_monitoring: true
  enable_performance_tracking: true
  
  # Component Dependencies
  dependencies:
    numpy: ">=1.21.0"
    scipy: ">=1.7.0"
    torch: ">=1.9.0"  # Optional for GPU
    cupy: ">=9.0.0"   # Optional for GPU

# Development Settings
development:
  enable_debug_mode: false
  enable_profiling: false
  enable_testing: false
  enable_mock_data: false
  
  # Testing Configuration
  testing:
    enable_unit_tests: true
    enable_integration_tests: true
    enable_performance_tests: true
    test_timeout: 60.0 